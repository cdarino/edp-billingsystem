export default function Card({children}) {
    const styles = {
        backgroundColor: "#FFFFFF",
        padding: "12px",
        borderRadius: "12px"
    }
    
    return (
        <div style={styles}>
           
        </div>
    )
}